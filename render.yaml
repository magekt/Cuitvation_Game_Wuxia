services:
  - type: web
    name: cultivation-heavenly-dao
    env: node
    buildCommand: npm run build
    startCommand: npm start
    plan: free
    envVars:
      - key: NODE_OPTIONS
        value: --max-old-space-size=400
