## 2026-09-22 - Map Grid Tile ARIA & Focus States
**Learning:** Dense custom interactive grid maps require explicit dynamic ARIA labels containing coordinate & location context for screen reader users, along with high-contrast `focus-visible` outlines so keyboard navigation across tiles remains clear.
**Action:** Always pair tile map buttons with dynamic `aria-label`s and `focus-visible:ring-2` outline states.
